apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: zybes-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: zybes-svc
                port:
                  number: 80
